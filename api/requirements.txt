# MataBumi API Dependencies for Vercel Serverless

# Web framework and API
fastapi==0.110.0
mangum==0.17.0

# No other dependencies needed for API endpoints
# (numpy, scipy, etc. are only for data pipeline, not API)
